# Firebase and Google Cloud environment variables
.env
.env.local
.env.*

# Note: firebase-config.js is included in the repo since it contains public config
# Only exclude files with private keys or secrets
